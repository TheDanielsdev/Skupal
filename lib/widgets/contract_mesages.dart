import 'package:flutter/material.dart';

class ContractMessages extends StatelessWidget {
  const ContractMessages({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
