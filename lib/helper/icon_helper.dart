import 'package:flutter/material.dart';

class IconHelper {
  static const Icon Time = Icon(Icons.access_time);
  static const Icon File = Icon(Icons.attach_file);
  static const Icon Dot = Icon(Icons.circle);
  static const Icon Bids = Icon(Icons.history_edu_outlined);
}
